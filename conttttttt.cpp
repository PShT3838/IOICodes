#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define all(x) x.begin(), x.end()
#define mk make_pair

const ll maxn = 2e5 + 5;
ll a[maxn];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll t;
    cin >> t;
    while (t--){
        ll n ,q;
        cin >> n >> q;
        for (int i = 0;i < n;i++){
            cin >> a[i];
        }
        while (q--){
            ll l, r;
            cin >> l >> r;
            
        }
    }

    system("pause");
    return 0;
}